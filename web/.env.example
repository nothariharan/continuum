# Base URL of the deployed Continuum Query API (the Docker runtime in deploy/).
# Leave unset for local dev (defaults to http://127.0.0.1:8080) or to run the
# UI in clearly-labelled DEMO mode when no backend is reachable.
NEXT_PUBLIC_CONTINUUM_API=https://your-continuum-api.example.com
